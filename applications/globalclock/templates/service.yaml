apiVersion: v1
kind: Service
metadata:
  name: "globalclock"
  labels:
    {{- include "globalclock.labels" . | nindent 4 }}
spec:
  type: "ClusterIP"
  ports:
    - port: 8080
      targetPort: "http"
      protocol: "TCP"
      name: "http"
  selector:
    {{- include "globalclock.selectorLabels" . | nindent 4 }}
